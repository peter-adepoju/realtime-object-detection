# Real-Time Object Detection and Counting — Requirements
# Python 3.9+ recommended (tested on 3.10)

# ── Core deep learning & detection ──────────────────────────────────────────
ultralytics>=8.0.0          # YOLOv8 — model loading, inference, utilities
torch>=2.0.0                # PyTorch backend (CPU version; for GPU see below)
torchvision>=0.15.0         # Vision utilities used internally by ultralytics

# GPU (optional) — replace torch/torchvision with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# ── Video and image processing ───────────────────────────────────────────────
opencv-python>=4.7.0        # cv2 — video I/O, frame manipulation, drawing
Pillow>=9.5.0               # PIL — image loading, format conversion

# ── Data handling ────────────────────────────────────────────────────────────
numpy>=1.24.0               # Array operations
pandas>=2.0.0               # Results tables, metric aggregation

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib>=3.7.0           # Plotting — FPS curves, count histograms, comparisons
seaborn>=0.12.0             # Statistical plots — heatmaps, distribution plots

# ── System profiling ─────────────────────────────────────────────────────────
psutil>=5.9.0               # Memory usage profiling (RAM)
py-cpuinfo>=9.0.0           # CPU info logging for reproducibility

# ── Streamlit dashboard ──────────────────────────────────────────────────────
streamlit>=1.28.0           # Interactive web dashboard
plotly>=5.15.0              # Interactive plots in Streamlit

# ── Jupyter notebook environment ─────────────────────────────────────────────
jupyterlab>=4.0.0           # Notebook server
ipywidgets>=8.0.0           # Progress bars and interactive widgets
tqdm>=4.65.0                # Progress bars in scripts

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.4.0               # Test runner
pytest-cov>=4.1.0           # Coverage reports

# ── Utilities ────────────────────────────────────────────────────────────────
pyyaml>=6.0                 # Config file loading
requests>=2.31.0            # Downloading sample videos / weights
python-dotenv>=1.0.0        # Environment variable management
tabulate>=0.9.0             # Nicely formatted tables in terminal output
